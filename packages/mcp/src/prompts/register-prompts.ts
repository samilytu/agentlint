import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerAgentLintPrompts(_server: McpServer): void {
  // Prompts will be added in FAZ 1 if needed.
  // The new tool-first architecture relies on tools + resources rather than prompts.
}
