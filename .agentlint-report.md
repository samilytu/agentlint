# Workspace Autofix Plan

Root: `C:\Users\samil\Desktop\agentlint`

Found 24 artifact file(s), 0 type(s) missing.

---

## Discovered artifacts

| File | Type | Size | Status |
| --- | --- | ---: | --- |
| `.windsurf\rules\deneme-rule1.md` | rules | 943B | OK |
| `.windsurf\skills\frontend\SKILL.md` | skills | 4277B | Missing 5 sections |
| `AGENTS.md` | agents | 4074B | Missing 6 sections |
| `docs\great_plan.md` | plans | 23947B | Missing 4 sections |
| `examples\github-action.yml` | agents | 476B | Missing 6 sections |
| `examples.md` | agents | 8729B | Missing 5 sections |
| `fixtures\bad-agents.md` | agents | 3593B | Missing 5 sections |
| `fixtures\bad-plans.md` | plans | 2296B | Missing 2 sections |
| `fixtures\bad-rules.md` | rules | 1533B | Missing 2 sections |
| `fixtures\bad-skills.md` | skills | 2532B | Missing 3 sections |
| `fixtures\bad-workflows.md` | workflows | 1712B | Missing 3 sections |
| `fixtures\good-agents.md` | agents | 4453B | Missing 2 sections |
| `fixtures\good-plans.md` | plans | 3476B | Missing 2 sections |
| `fixtures\good-rules.md` | rules | 2772B | OK |
| `fixtures\good-skills.md` | skills | 3808B | OK |
| `fixtures\good-workflows.md` | workflows | 2920B | Missing 1 sections |
| `fixtures\workspace\.cursor\rules\code-style.md` | rules | 2772B | OK |
| `fixtures\workspace\.windsurf\skills\testing\SKILL.md` | skills | 3808B | OK |
| `fixtures\workspace\AGENTS.md` | agents | 4453B | Missing 2 sections |
| `fixtures\workspace\docs\deploy-workflow.md` | workflows | 2920B | Missing 1 sections |
| `fixtures\workspace\docs\roadmap.md` | plans | 3476B | Missing 2 sections |
| `packages\cli\README.md` | agents | 3660B | Missing 6 sections |
| `packages\mcp\README.md` | agents | 3712B | Missing 6 sections |
| `README.md` | agents | 10104B | Missing 6 sections |



## Action plan

1. **Fix `.windsurf\skills\frontend\SKILL.md`**: This skills file is missing sections: `scope`, `inputs`, `step`, `verification`, `safety`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "skills" })`.
2. **Fix `AGENTS.md`**: This agents file is missing sections: `quick commands`, `repo map`, `working rules`, `verification`, `security`, `do not`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
3. **Fix `docs\great_plan.md`**: This plans file is missing sections: `non-goals`, `risk`, `phase`, `verification`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "plans" })`.
4. **Fix `examples\github-action.yml`**: This agents file is missing sections: `quick commands`, `repo map`, `working rules`, `verification`, `security`, `do not`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
5. **Fix `examples.md`**: This agents file is missing sections: `quick commands`, `repo map`, `working rules`, `security`, `do not`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
6. **Fix `fixtures\bad-agents.md`**: This agents file is missing sections: `quick commands`, `repo map`, `working rules`, `security`, `do not`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
7. **Fix `fixtures\bad-plans.md`**: This plans file is missing sections: `non-goals`, `evidence`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "plans" })`.
8. **Fix `fixtures\bad-rules.md`**: This rules file is missing sections: `don't`, `security`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "rules" })`.
9. **Fix `fixtures\bad-skills.md`**: This skills file is missing sections: `purpose`, `inputs`, `step`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "skills" })`.
10. **Fix `fixtures\bad-workflows.md`**: This workflows file is missing sections: `goal`, `failure`, `safety`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "workflows" })`.
11. **Fix `fixtures\good-agents.md`**: This agents file is missing sections: `repo map`, `working rules`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
12. **Fix `fixtures\good-plans.md`**: This plans file is missing sections: `non-goals`, `evidence`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "plans" })`.
13. **Fix `fixtures\good-workflows.md`**: This workflows file is missing sections: `goal`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "workflows" })`.
14. **Fix `fixtures\workspace\AGENTS.md`**: This agents file is missing sections: `repo map`, `working rules`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
15. **Fix `fixtures\workspace\docs\deploy-workflow.md`**: This workflows file is missing sections: `goal`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "workflows" })`.
16. **Fix `fixtures\workspace\docs\roadmap.md`**: This plans file is missing sections: `non-goals`, `evidence`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "plans" })`.
17. **Fix `packages\cli\README.md`**: This agents file is missing sections: `quick commands`, `repo map`, `working rules`, `verification`, `security`, `do not`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
18. **Fix `packages\mcp\README.md`**: This agents file is missing sections: `quick commands`, `repo map`, `working rules`, `verification`, `security`, `do not`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.
19. **Fix `README.md`**: This agents file is missing sections: `quick commands`, `repo map`, `working rules`, `verification`, `security`, `do not`. Read the file, then add the missing sections following the guidelines from `agentlint_get_guidelines({ type: "agents" })`.

## Guidelines references

For each artifact type mentioned in the action plan, call the corresponding guidelines tool:

- **rules**: `agentlint_get_guidelines({ type: "rules" })` or read resource `agentlint://guidelines/rules`
- **skills**: `agentlint_get_guidelines({ type: "skills" })` or read resource `agentlint://guidelines/skills`
- **agents**: `agentlint_get_guidelines({ type: "agents" })` or read resource `agentlint://guidelines/agents`
- **plans**: `agentlint_get_guidelines({ type: "plans" })` or read resource `agentlint://guidelines/plans`
- **workflows**: `agentlint_get_guidelines({ type: "workflows" })` or read resource `agentlint://guidelines/workflows`

---

## Instructions for the LLM

Execute the action plan above step by step:
1. For each step, read the referenced file (if it exists).
2. Call `agentlint_get_guidelines` for the artifact type to get the full specification.
3. If you need to scan the codebase for project-specific information (tech stack, scripts, etc.), do so before writing.
4. Apply the changes directly using your file editing capabilities.